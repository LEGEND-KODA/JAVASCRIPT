let numbers = [ 1, 2, 3, 4, 5]
let fruits = [ "apple", "banana", "orange"]

console.log(numbers[0]); //output: 1
console.log(fruits.length); //output: 3


let girls = ["Alice", "Bob", "Charlie" , "Afoma" , 
    "Praise" , "Bright" , "Daniella" , "Tonia" , "Blessing"];
    console.log(girls); 
    
    //removing the last element of the array
    girls.pop()
    console.log(girls);

    //removing the first element of the array
    girls.shift()
    console.log(girls);

    //adding an element to the end of the array
    girls.push("Eunice")
    console.log(girls);

    //adding an element to the beginning of the array
    girls.unshift("Faith")
    console.log(girls);





    let students = ["Daniel", "Mary", "Samuel" , "Blessing" , "Joshua"];
    console.log(students);

    //Adding David to the end of the queue
    students.push("David");
    console.log(students);

    //Adding Esther to the beginning of the queue
    students.unshift("Esther");
    console.log(students);

    //Removing the last element of the queue
    students.pop();
    console.log(students);

    //Removing the first element of the queue
    students.shift();
    console.log(students);

    //Finding the index of an element in the array
    console.log(students.indexOf("Samuel"));
    console.log(students.indexOf("David"));
    console.log(students.indexOf("Mary"));
    console.log(students.length);

    //Adding Micheal to the beginning of the queue
    students.unshift("Micheal");
    console.log(students);

    //Adding Grace to the end of the queue
    students.push("Grace");
    console.log(students);

    //Removing the first element of the queue
    students.shift();
    console.log(students);

    //Removing the last element of the queue
    students.pop();
    console.log(students);

    //Finding the index of an element in the array
    console.log(students[0]);
    console.log(students[2]);

    //Finding the index of an element in the array
    console.log(students[4]);

    //Finding the length of the array
    console.log(students.length);

    //Finding the index of an element in the array
    console.log(students.indexOf("Samuel"));
    console.log(students.indexOf("David"));
    console.log(students.indexOf("Esther"));
    console.log(students.indexOf("Grace"));
    console.log(students.length);